# Cloud Bigtable KFRU Init

1. Repository auf Google Cloud Shell klonen
2. ```sudo apt install maven```
3. ```mvn package```
4. ```mvn exec:java -Dbigtable.projectID=kfrufkonrad -Dbigtable.instanceID=kfrubigtable```
